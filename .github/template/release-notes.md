## 🚀 ${REPO_NAME} ${RELEASE_TAG} 发布说明

> 由 GitHub Actions 自动生成于 ${RELEASE_TIME} · 构建记录 [#${GITHUB_RUN_NUMBER}](${RUN_URL})

### 📋 发版信息

| 项目 | 内容 |
| :--- | :--- |
| **版本标签** | `${RELEASE_TAG}` |
| **发版时间** | ${RELEASE_TIME} |
| **触发方式** | `${GITHUB_EVENT_NAME}` |
| **发布人** | @${GITHUB_ACTOR} |
| **分支 / Tag** | `${GITHUB_REF_NAME}` |
| **提交 SHA** | [`${SHORT_SHA}`](${REPO_URL}/commit/${GITHUB_SHA}) |
| **Node.js** | `${NODE_VERSION}` |

### 📝 变更说明

${RELEASE_CHANGES}

---

📦 **相关链接**: [GitHub 仓库](${REPO_URL}) · [Release 页面](${RELEASE_URL}) · [构建记录](${RUN_URL})
